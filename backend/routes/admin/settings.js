const express = require('express');
const router = express.Router();

// Placeholder for settings routes
// Will be implemented in Phase 5

router.get('/', (req, res) => {
  res.json({ message: 'Settings routes - to be implemented in Phase 5' });
});

module.exports = router; 