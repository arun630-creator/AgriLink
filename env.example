# Backend Configuration
VITE_API_BASE_URL=http://localhost:5000/api

# Location Services (Free OpenStreetMap API - No API key required!)
# The app uses OpenStreetMap's Nominatim service which is completely free
# Rate limit: 1 request per second (sufficient for most use cases) 